name: adhyay_pdf_reader
description: A Cross-Platform PDF Reader APP
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0<4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.1.2
  syncfusion_flutter_pdfviewer: ^31.1.17
  shared_preferences: ^2.3.3
  path_provider: ^2.1.3
  universal_io: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true